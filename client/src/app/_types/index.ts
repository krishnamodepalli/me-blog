export interface IPostPreview {
  id: string;
  title: string;
  createdAt: string;
}

export interface IDraft {
  id: string;
  bannerURL: string;
  title: string;
  content: string;
}

export interface IPost {
  id: string;
  bannerURL: string;
  title: string;
  content: string;
}
